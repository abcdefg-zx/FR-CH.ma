services:
  - type: web
    name: zhongfa-translator
    runtime: python
    rootDir: translator_app/backend
    installCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
